$(document).ready(function(){
    $("table").DataTable({
        scrollX: false,
        scrollCollapse: true,
        autoWidth: true,
        paging: true,
        columnDefs: [{ 
            width: "50px", targets: -1, ordering: false, searching: false
        }],
    });
});